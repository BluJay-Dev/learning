#!/bin/bash

# ls exit code

#ls /not/here 
#echo "$?" '

# ping exit code


#HOST="googlerty.com"
#ping -c 1 $HOST
#if [ "$?" -eq "0" ]
#then 
#    echo "$HOST reachable" 
#else 
#    echo "$HOST unreachable."
#fi 

# Variable exit code
#HOST="google.com"
#ping -c 1 $HOST 
#RETURN_CODE="$?"

#if [ "$RETURN_CODE" -ne "0" ]
#then   
#    echo "$HOST unreachable"
#else 
#    echo "$HOST reachable"
#fi 